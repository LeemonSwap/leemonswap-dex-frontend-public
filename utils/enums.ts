export enum mainColor {
  green,
  yellow,
}

export enum buttonCollor {
  green,
  lightGreen,
  purple,
  yellow,
  white,
  darkPurple,
}

export enum tooltipDirection {
  top,
  right,
  bottom,
  left,
}
